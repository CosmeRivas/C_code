#include<stdio.h>
int maint(){
	int a, b;
	printf("entre a: ");
	scanf("%d", a);
	printf("entre b: ");
	scanf("%d",b);
	printf("le produit est positive");
	printf("le produit est negative");
	return 0;
}
